name: Backup DB

on:
  workflow_dispatch:

jobs:
  call-backup-db:
    runs-on: ubuntu-latest
    steps:
      - name : Echo var environments
        env: 
          JASYPT: ${{ secrets.MYTOKEN }}
        shell: bash 
        run: |
          echo $JASYPT
          echo "is it good?"