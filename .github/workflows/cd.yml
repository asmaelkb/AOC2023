name: CD
on:
  push:
    branches:
      - main

jobs:

  change-directory:
    name: change-directory
    runs-on: ubuntu-latest

    steps:
        - name: Checkout code
          uses: actions/checkout@v4
        - if: ${{ hashFiles('README.md') != '' }}
          run: | 
            echo YOUPI


