name: CD
on:
  push:
    branches:
      - main

jobs:

  change-directory:
    name: change-directory
    runs-on: ubuntu-latest

    steps:
        - run: | 
            echo $(pwd)
        - run: |
            cd day1
        - run: |
            echo $(pwd)
  
  change-directory2:
    name: change-directory
    runs-on: ubuntu-latest

    steps:
        - run: |
            cd ..
        - run: |
            echo $(pwd)
